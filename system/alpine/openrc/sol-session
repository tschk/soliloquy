#!/sbin/openrc-run

name="soliloquy browser session"
description="Launch the immutable browser appliance with Servo"
command="/usr/local/bin/sol-session-start"
command_background="yes"
pidfile="/run/sol-session.pid"

depend() {
    need localmount
    need seatd
    need sold
}
